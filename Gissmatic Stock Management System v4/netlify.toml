[build]
  command = "npm run build"
  publish = "dist"

# The following redirect is intended for Single Page Applications (SPAs).
# It rewrites all paths to index.html so React Router can handle them.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
